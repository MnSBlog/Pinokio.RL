# --- Modules ---
runner_name: "StepRunner" # path about episode / gym / ray_tune
network_name: "Swimmer-v4" # custom_network_path / torchvision.models(pre-trained)
env_name: "Swimmer-v4" # Environment name (include gym.env)
agent_name: "PPO" # Algorithm name